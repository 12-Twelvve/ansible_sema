---
- hosts: my_servers
  become: true
  tasks:
    - name: install apache2
      apt: name=apache2 update_cache=yes state=latest
    - name: Print OS info
      ansible.builtin.package:
        name: apache2
